{
  "name": "uw-workbench",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace uw-workbench-backend",
    "dev:frontend": "npm run dev --workspace uw-workbench-frontend",
    "build": "npm run prisma:generate --workspace uw-workbench-backend && npm run build --workspace uw-workbench-frontend",
    "typecheck": "npm run typecheck --workspace uw-workbench-backend && npm run typecheck --workspace uw-workbench-frontend",
    "test": "npm run test --workspace uw-workbench-backend",
    "test:coverage": "npm run test:coverage --workspace uw-workbench-backend"
  },
  "engines": {
    "node": ">=20"
  }
}
