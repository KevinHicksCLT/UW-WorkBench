# Drift Log — changes to `develop` while `refactor` is open

Feature development on the old codebase is frozen for the duration of the refactor
(charter Task 19). Any change that must land on `develop` anyway is logged here and
replicated onto `refactor` before the cutover merge.

| Date | develop commit | Description | Replicated on refactor? |
| --- | --- | --- | --- |
| — | — | _none yet_ | — |
