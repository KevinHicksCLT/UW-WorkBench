/goal We are to refactor this codebase to meet enterprise best designs, practices, coding standards, standards, regulations, and use the right tools for the correct jobs.



The first thing is to break this monolithic front code into modern manageable work able code - meaning:

-goal: ensure nothing is hardcoded from a data standpoint all data must be pulled from the db to ensure this can easily be set up from multiple companies and provide them with the level editable details needed
-reason: we need to be able to onboard new companies quickly and this scaffolding needs to support there data in what every format it comes and how it needs to be organized hardcoding and a one size fits all approach will not work in this application
-outcome: we can easily modify structure, organization preferences all from the db pertaining to company, not hardcoded attributes which lock down this. 

- goal: review and implement across all screens best design practices, best coding practices, ensuring that scalability, performance and user experience is kept in mind
- reason: there is some performance and best practices compliance issues i have saw in this app, not keeping in mind reusability
-outcome: the application runs and operates like an well oiled machine and the same functionality we have no still exists

- goal: we need to create a internal component lib for commonly use component
- reason: we have components that are following similar concepts and approaches but look slightly different and every page, we want to be efficient there is no reason to spend unnecessary lines, workability, and scalability
- outcome: we are to have a comprehensive component library that contains all the commonly used components in order to reuse, they must represent the same layout, functionality and data when complete
- examples: List/Tables (use standards as a base line), maps views, checklist/testing matrics, cards, headers, logo, buttons.

- goal: we are to be organized with our code and that it appears nicely ensure things are logically grouped including supporting documents/code/configs, no 500+ line files use helpers, utils, hooks etc to make the code organized and manageable
- reason: the code is a jumbled mess and impossible to understand 
- outcome: to ensure understandability, manageability, workability, scalability code is nicely organized and the functionality is work the same as before

- goal: all code is to be well documented 
- reason: we have minimal documentation at the moment
-outcome anyone who looks at this code base is blown away and understands every single piece of it

- goal: lint scans and SonarQube scans are set up to run on commit and will not allow push with any issues:
- reason: to maintain a healthy codebase and to ensure that the issues are resolved.
-out come: no warnings or errors appear in the code base ever.

-goal: all source code is unit tested to over 80% coverage
-reason: no unit tests exist at the moment
-outcome: to ensure that the code is functional as expected and new implementations and changes do not break existing functionality.

-goal: remove all dead code
-reason I believe that there is dead code in the application that was never cleaned up
-outcome to ensure we keep the codebase clean and organized and not carry around extra weight


goal: api ensure we are using the correct api structure and packages, and libraries to ensure the best performance and scalability of this app
reason: e facing super slow weight times and I want to quickly be able to make api changes if need be so having the reusable and configurable are important as well for scalability and performance
outcome: the app runs like a well oiled machine if updates need to happed

- goal: review and implement across all apis best design tices, best coding practices, ensuring that scalability, performance and user experience is kept in mind
- reason: there is some performance and best practices compliance issues i have saw in this app, not keeping in mind reusability, we are experiencing very very long wait times for the screens.
-outcome: the application runs and operates like an well oiled machine and the same functionality we have no still exists

goal db is prism the correct and most performative tool? for scalability for performance for ease to work on?
reason: i know nothing about dbs but prism seems clunkly and is causing a bunch of random file creations, we will also be on boarding a team soon and want the ease to set up across multiple devices
outcome: for database updates adn build we are using the right tool and our current implementation copys over seamlessly

goal: clean up dead or unused db tables, data which is not surfaced anywhere in the UI
reason: to ensure we are not carrying around extra weight and not bloting the db
outcome: the db contains only the data it needs to render as of now and nothing is effect on the screen if we remove old data functionality countines working as expected

goal: all the data in the db is pulled from a single source of truth there is no duplicates in the database.
reason: to ensure data consistency and that we have data out of sync in the application.
outcome: all data reads as before but pulled from a single table of truth by assocations
