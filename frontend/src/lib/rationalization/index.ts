/**
 * Barrel for the rationalization lib — types (§6.2 DTO shapes), vocabulary
 * derivations (3-A: everything renders from vocabulary rows) and pure
 * derivations. Import from '../../lib/rationalization' as before; the module
 * was split from a single file to stay under the 500-line ceiling.
 */
export * from './types';
export * from './vocabulary';
export * from './derive';
